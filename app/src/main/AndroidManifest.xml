<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enroute.enroute">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="com.androidtutorialpoint.mymapsappsdirection.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBSuFO5k_nS7L7-MsHBaaJQLKsdwbD0A-c" />



        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".log_in" />
        <activity android:name=".sign_up" />
        <activity android:name=".SpecialOffer" />
        <activity android:name=".SpecialActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".UserSignUpActivity" />
        <activity android:name=".UserEditActivity" />
        <activity android:name=".SuggestActivity" />
        <activity android:name=".RestLoginActivity" />
        <activity android:name=".RestActivity" />
        <activity android:name=".RestSignUpActivity" />
        <activity android:name=".CreateRestaurantActivity" />
        <activity android:name=".SpecialOffersActivity" />

        <activity android:name=".RestEditActivity"/>
        <activity android:name=".RestaurantRecommendationActivity"/>
        <activity android:name=".PreferenceTabsActivity"/>
        <activity android:name=".WaitForCuisinesActivity"/>
        <activity android:name=".UserSubmitInfoActivity"/>

        <!-- Services for push notification -->
        <service

            android:name=".MyAndroidFirebaseMsgService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <service

            android:name=".MyAndroidFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>


    </application>

</manifest>